const width = prompt("Please enter the width of the box:");
const height = prompt("Please enter the height of the box:");
const depth = prompt("Please enter the depth of the box:");

const volume = width * height * depth;
document.write(`The volume of the box is: ${volume}`);