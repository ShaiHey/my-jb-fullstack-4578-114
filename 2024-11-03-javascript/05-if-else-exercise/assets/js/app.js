const num1 = +prompt(`Please enter a number : `);
const num2 = +prompt(`Please another number : `);

// parseInt() - Integer 
// parseFloat() - Float
// + - is a conversion operator if placed before a string operand

if(num1 > num2){
    console.log(num1);
} else {
    console.log(num2);
}